import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DestinationService {

  constructor() { }

  //TODO
  // GetAll(): Destination[];
  // DeleteById(id: number): boolean;
  // Update(destination: Destination): boolean;
  // Create(destination: Destination): boolean;

}
